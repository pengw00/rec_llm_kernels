# Neuron tests

The Neuron/Inf2 unit tests are shipped inside the `rec_llm_kernels_neuron` package and can be run on an Inf2 host via:

```bash
rec-llm-kernels-neuron-test -q
```

`legacy_tests_inf2/` contains older repo-local tests kept for reference.

