# `rec-llm-runtime`

Runtime scaffolding for a decode-only MVP (RoPE + paged KV cache + paged attention decode wiring).

This package is CPU-first and can optionally call into the CUDA extension package if installed.

